define([
    'BackWidgets/Core/Base/DataModel',
    'BackWidgets/Core/Tools/Parsers'
],
function (BaseDataModel, Parsers) {

    return {
        router: {
            redirect: {
                // 'FROM':'TO'
                //'Source': 'Destination/concretedestination',
            }
        }
    };
});