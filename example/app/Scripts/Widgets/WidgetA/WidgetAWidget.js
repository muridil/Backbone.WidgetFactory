//Menu1 - widget
define([
        'BackWidgets/Core/CoreLib',
        'Globals',
    ],
    function(Core, globals) {
        return Core.Base.Widget.extend({
            execute: function() {

            }
        });
    });